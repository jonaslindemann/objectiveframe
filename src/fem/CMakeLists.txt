FILE(GLOB SOURCE_FILES *.cpp)
FILE(GLOB INCLUDE_FILES ${PROJECT_SOURCE_DIR}/include/ofem/*.h) 

ADD_LIBRARY(fem ${INCLUDE_FILES} ${SOURCE_FILES})
set_target_properties(fem PROPERTIES DEBUG_POSTFIX d) 
