FILE(GLOB SOURCE_FILES *.cpp)
FILE(GLOB INCLUDE_FILES ${PROJECT_SOURCE_DIR}/include/imguifd/*.h) 

ADD_LIBRARY(imguifd ${INCLUDE_FILES} ${SOURCE_FILES})
set_target_properties(imguifd PROPERTIES DEBUG_POSTFIX d)

