FILE(GLOB SOURCE_FILES *.cpp) 
FILE(GLOB INCLUDE_FILES ${PROJECT_SOURCE_DIR}/include/ofui/*.h) 

ADD_LIBRARY(ofui ${INCLUDE_FILES} ${SOURCE_FILES})
set_target_properties(ofui PROPERTIES DEBUG_POSTFIX d) 
